require "spec_helper"

RSpec.describe Sequel::Bulk::Audit do
  it "has a version number" do
    expect(Sequel::Bulk::Audit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
